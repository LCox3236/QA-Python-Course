""" Must have top comment """
def count(sequence, item):
    """
    Must include comments in code
    """
    counter = 0
    for letter in sequence:
        if letter == item:
            counter += 1
    return counter
